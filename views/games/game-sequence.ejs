<%- include('../partials/header'); -%>
<head>
    <link rel="stylesheet" type="text/css" href="/../styles/game-sequence.css">
</head>
<body>
<a href="/games" class="btn back"><i class="fas fa-arrow-left"></i>До ігор</a>
<div id='controls' class="container">
    <label for='timeLimit'>Обмеження в часі(1-5)</label>
        <% if (user.status === 'Початківець') { %>
            <p>Рекомендовано: 2</p>
        <% } else if (user.status === 'Середній') { %>
            <p>Рекомендовано: 1</p>
        <% } else { %>
            <p>Рекомендовано: 2</p>
        <% } %>
    <input type='number' id='timeLimit' name='timeLimit' min='1' max='5'>
    <label for='amount'>Кількість кружечків(3-20)</label>
        <% if (user.status === 'Початківець') { %>
            <p>Рекомендовано: 3</p>
        <% } else if (user.status === 'Середній') { %>
            <p>Рекомендовано: 5</p>
        <% } else { %>
            <p>Рекомендовано: 10</p>
        <% } %>
    <input type='number' id='amount' name='amount' min='3' max='20'>
    <a class="btn btn-game" id='start'>Почати</a>
</div>
<div class="container">
    <canvas id='canvas'></canvas>
</div>
<script src='/../js/game-sequence.js'></script>
</body>
<%- include('../partials/footer'); -%>